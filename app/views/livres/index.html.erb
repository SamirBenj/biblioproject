<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
  </head>

  <body>
    <h1>Hello samir</h1>
    <% @leslivres.each do |livre| %>
      <div> <%= livre.domain %> </div>
    <div> <%= livre.titre %> </div>
    <div> <%= livre.auteur %> </div>
      <div> <%= link_to "Voir livre", livre_path(livre) %> </div>
    <!--<div> <%= link_to "ajouter nouveau livre", new_livre_path() %> </div>-->
      <div> <%= link_to "modifier le livre", edit_livre_path(livre) %> </div>
      <div> <%= button_to "supprimer nouveau livre", livre_path(livre), method: :delete %> </div>
   <hr>
    <%end%>
  </body>
</html>


<%# <h1>ekekes</h1> %>